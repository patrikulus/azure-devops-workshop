# Variables to set in Azure Pipelines:
# SolutionPath: path to .sln file
# Subscription: Azure subscription ID

trigger: 
  - master

pool:
  name: Azure Pipelines
  vmImage: windows-2019

variables:
  ServiceConnection: 'workshop-azure'

steps:
- template: templates/build.yml
  parameters:
    solutionPath: $(SolutionPath)

- template: templates/deploy.yml
  parameters:
    serviceConnection: $(ServiceConnection)
    subscription: $(Subscription)
    resourceGroup: 'workshop-test-pl07'
    appServiceName: 'workshop-test-app-pl07'
    appServicePlanName: 'workshop-test-plan-pl07'

- template: templates/deploy.yml
  parameters:
    serviceConnection: $(ServiceConnection)
    subscription: $(Subscription)
    resourceGroup: 'workshop-prod-pl07'
    appServiceName: 'workshop-prod-app-pl07'
    appServicePlanName: 'workshop-prod-plan-pl07'
  
    
